# Touch + vibrator are loaded in order by rain-bringup.service
# (spi-bitbang → gpio-shared-proxy → fts_spi → spi-gpio → ff-memless → gpio-vibra).
# Do not list modules here: order matters and spi-geni must stay unloaded.
