[Manager]
# Keep boot snappy: wlan0 is NOT present until manual qcom-wifi-start.
# Conflicting longer timeouts elsewhere caused ~30s stalls on missing wlan0.
DefaultTimeoutStartSec=30s
DefaultTimeoutStopSec=15s
DefaultDeviceTimeoutSec=5s
