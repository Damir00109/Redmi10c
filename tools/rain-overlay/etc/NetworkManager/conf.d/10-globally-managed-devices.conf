# Override Ubuntu package /usr/lib/NetworkManager/conf.d/10-globally-managed-devices.conf
# Stock Ubuntu also manages type:gsm/cdma — that pokes ModemManager + QRTR on this
# board and contributes to hangs/reboots. Only allow Wi-Fi under NM.
[keyfile]
unmanaged-devices=*,except:type:wifi
