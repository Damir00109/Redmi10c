# Ubuntu ships default-wifi-powersave-on.conf with wifi.powersave=3 (enable).
# On rain/fog (ath10k_snoc + MPSS) powersave thrash reboots the board.
# Values: 2=disable, 3=enable. Keep this filename so it replaces the stock file.
[connection]
wifi.powersave=2
